apply plugin: 'java'

repositories {
    jcenter()
    maven { url 'https://jitpack.io' }
}

def psuVersion="c2adaf623d"

dependencies {
    //compile 'com.github.xzzpig.PigSubUtils:Logger:'+psuVersion
    //compile 'com.github.xzzpig.PigSubUtils:PackageSocket:'+psuVersion
    //compile 'com.github.xzzpig.PigSubUtils:Other:'+psuVersion
    compile group: 'org.apache.logging.log4j', name: 'log4j-api', version: '2.8.2'
    compile group: 'org.apache.logging.log4j', name: 'log4j-core', version: '2.8.2'
    testCompile 'junit:junit:4.12'
}
